# Sandbox Submit Disabled Behavior After 10K8ZJG

- `build_sandbox_submit_request()` builds a local request only.
- submit_sandbox_order_disabled() always raises disabledsandboxsubmiterror.
- Live trading remains disabled.
- The sandbox submit interface is metadata and guard logic only.
