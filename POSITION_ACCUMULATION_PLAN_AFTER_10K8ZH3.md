# Position Accumulation Plan (After 10K8ZH3)

## Concepts

- **Warehousing** – gradual entry over time to avoid signalling.
- **Thesis‑break stop** – exit if the hypothesis is invalidated.
- **Exposure‑aware sizing** – reduce size as correlation exposure grows.
- **Bayesian update** – update posterior probability after each partial fill.

## Implementation Status

**Not implemented.** Will be added in `src/core/portfolio.py` and `src/services/decision_engine.py` when Stage 3 begins.
