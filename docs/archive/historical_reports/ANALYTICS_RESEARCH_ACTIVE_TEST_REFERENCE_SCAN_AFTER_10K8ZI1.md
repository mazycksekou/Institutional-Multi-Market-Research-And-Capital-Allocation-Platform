# Analytics/Research Active Test Reference Scan After 10K8ZI1

Active test references were scanned for legacy wrapper ownership assumptions.

Classifications:
- runtime import: canonical modules under `src.analytics` / `src.research`
- test import: canonical regression tests and proof tests
- monkeypatch or mock targets: package-level compatibility helpers only
- historical proof evidence: retained docs and comments from earlier phases
- compatibility export: `model_governance` and `automation_scheduler` package facades
- string-only metadata: legacy file names mentioned in docs
- doc-only evidence: historical references that do not drive runtime ownership

Doc-only evidence is tracked separately and does not count as a runtime dependency.
