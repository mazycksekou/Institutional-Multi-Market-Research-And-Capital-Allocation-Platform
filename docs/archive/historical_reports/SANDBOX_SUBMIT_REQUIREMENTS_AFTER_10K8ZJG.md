# Sandbox Submit Requirements After 10K8ZJG

- Canonical `OrderRequest` and `ExecutionRequest` contracts are required.
- `ApprovalState` is required.
- `SandboxBrokerDescriptor` is required.
- The request is live-shaped but disabled.
