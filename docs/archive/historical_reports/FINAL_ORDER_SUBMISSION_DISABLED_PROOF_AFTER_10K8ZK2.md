# Final Order Submission Disabled Proof After 10K8ZK2

- `submit_live_order_disabled()` always raises a disabled error.
- `submit_sandbox_order_disabled()` always raises a disabled error.
- No order submission path is live.

Order submission remains blocked.
