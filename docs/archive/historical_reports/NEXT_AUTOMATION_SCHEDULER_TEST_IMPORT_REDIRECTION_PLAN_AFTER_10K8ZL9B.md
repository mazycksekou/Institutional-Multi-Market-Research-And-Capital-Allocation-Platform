# Next Automation Scheduler Test Import Redirection Plan

The remaining work is test-side redirection:

- 198 active test files
- 524 direct test import statements

Planned next steps:

- replace active scheduler test imports with canonical `src.*` imports
- keep historical proof references as documentation only
- preserve bridge/support compatibility files until their own proof phase
- avoid deleting scheduler code until active test and runtime dependencies are gone

