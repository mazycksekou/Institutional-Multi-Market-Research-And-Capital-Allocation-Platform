# Live Approval Disabled Behavior After 10K8ZJ7

The approval gate is scaffolded, but live behavior remains disabled.

Rules:

- `require_live_approval()` raises unless all required approval requirements are satisfied.
- require_live_approval() raises unless all required approval requirements are satisfied.
- Default approval state blocks live activation.
- An approved local state can pass evaluation only.
- Evaluation does not submit orders, create accounts, or activate a broker client.
- No environment variables or credentials are read at import time.
