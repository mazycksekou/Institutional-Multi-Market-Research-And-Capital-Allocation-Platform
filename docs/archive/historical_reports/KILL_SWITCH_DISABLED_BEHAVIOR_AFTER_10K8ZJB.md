# Kill Switch Disabled Behavior After 10K8ZJB

Rules:

- The default kill switch blocks live activation.
- `require_kill_switch_clear()` raises while the kill switch is engaged.
- No live execution or account changes occur.
