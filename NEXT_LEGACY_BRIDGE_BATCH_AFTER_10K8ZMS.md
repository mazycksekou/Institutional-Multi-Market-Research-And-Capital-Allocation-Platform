# Next Legacy Bridge Batch

## Recommended next step

Continue with the remaining legacy provider and event-type bridge modules that still support the security cluster:

- `src/automation_scheduler_legacy/provider_allowlist.py`
- `src/automation_scheduler_legacy/security_event_types.py`

These are the last obvious pieces of the security stack that can be folded into canonical `src.providers` and `src.security` modules.

