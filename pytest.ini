[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    smoke: tiny always-green slice
    changed: use pytest-testmon cache
    architecture_archive: archived migration-proof tests
