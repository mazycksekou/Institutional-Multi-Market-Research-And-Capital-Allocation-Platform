# Final Repository Map

The canonical executable repository surface now lives under `src/*`, with a clean import graph and no dead executable modules.

Total tracked Python files in the current scan: `1165`.

See also: `inventories/orphan_dead_code_inventory.json`.